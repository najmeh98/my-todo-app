export interface Theme {
  fontSize: {
    normal: string;
  };
  fontFamily: {
    MainFont: string;
  };
  fontWeight: { regular: string };
  color: {
    background: string;
    containerBackground: string;
    // overlayBackground: string;
    textcolor: string;
    buttoncolor: string;
    InputBgcolor: string;
  };
  boxShadow: string;
  borderRadius: string;
  height: number;
}
